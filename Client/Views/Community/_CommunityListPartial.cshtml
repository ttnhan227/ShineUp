@model List<Client.Models.CommunityViewModel>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-info">No communities found.</div>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-light">
                <tr>
                    <th>Community Name</th>
                    <th>Description</th>
                    <th>Created Date</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var community in Model)
                {
                    <tr>
                        <td>@community.Name</td>
                        <td>@(string.IsNullOrEmpty(community.Description) ? "No description" : community.Description)</td>
                        <td>@community.CreatedAt.ToString("dd/MM/yyyy")</td>
                        <td>
                            <a href="@Url.Action("Details", "Community", new { communityId = community.CommunityID })" 
                               class="btn btn-sm btn-outline-primary">
                                <i class="fas fa-eye"></i> View Details
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}