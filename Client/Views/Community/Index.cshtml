@model List<Client.DTOs.CommunityDTO>
@{
    ViewData["Title"] = "Danh sách cộng đồng";
}

<h2>@ViewData["Title"]</h2>

<a href="@Url.Action("Create", "Community")" class="btn btn-success mb-3">Tạo cộng đồng mới</a>

@await Html.PartialAsync("_CommunityListPartial", Model)